<%= form_for @quiz do |f| %>
	<%= f.hidden_field :user_id, value: current_user.id %>
	<%= f.text_field :subject, placeholder: "Subject" %>
	<%= f.text_field :name, placeholder: "Quiz Name" %>
	<%= f.submit %>
<% end %>